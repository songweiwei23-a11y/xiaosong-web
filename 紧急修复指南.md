# 🚨 紧急修复指南

## 问题：网站无法访问

原因：可能是最近的部署导致构建失败或服务崩溃

---

## 立即执行的修复步骤

### 1. 登录服务器
\\\ash
ssh ubuntu@122.51.234.155
# 输入密码
\\\

### 2. 检查服务状态
\\\ash
pm2 status
pm2 logs xiaosong-web --lines 50
\\\

### 3. 如果服务已停止，重启它
\\\ash
cd /var/www/xiaosong-web
pm2 restart xiaosong-web
\\\

### 4. 如果重启失败，查看错误日志
\\\ash
pm2 logs xiaosong-web --err --lines 100
\\\

---

## 方案A：回滚到上一个工作版本（最安全）

\\\ash
cd /var/www/xiaosong-web

# 查看最近的提交
git log --oneline -5

# 回滚到之前的工作版本（a1aab27 - Next.js 16升级版本）
git reset --hard a1aab27

# 重新构建
npm run build

# 重启服务
pm2 restart xiaosong-web

# 检查状态
pm2 status
\\\

---

## 方案B：修复当前版本

如果你想保留最新代码，执行：

\\\ash
cd /var/www/xiaosong-web

# 拉取最新代码
git pull

# 清理缓存
rm -rf .next
rm -rf node_modules/.cache

# 重新安装依赖（如果需要）
npm install

# 构建（忽略 TypeScript 错误）
npm run build -- --no-type-check || npm run build

# 重启服务
pm2 restart xiaosong-web

# 查看日志
pm2 logs xiaosong-web --lines 20
\\\

---

## 方案C：降级 Next.js（如果方案B失败）

\\\ash
cd /var/www/xiaosong-web

# 回滚到 Next.js 14
npm install next@14.2.5

# 恢复 cookies API 改动
# 编辑 app/auth/callback/route.ts
nano app/auth/callback/route.ts
# 将 const cookieStore = await cookies() 改回 const cookieStore = cookies()

# 重新构建
npm run build

# 重启
pm2 restart xiaosong-web
\\\

---

## 检查清单

1. [ ] 登录服务器
2. [ ] 检查 pm2 status
3. [ ] 查看错误日志
4. [ ] 选择修复方案并执行
5. [ ] 验证网站可以访问
6. [ ] 截图状态给我

---

## 紧急联系

如果所有方案都失败了，请把以下信息发给我：

\\\ash
# 执行这些命令并截图
pm2 status
pm2 logs xiaosong-web --lines 50
cd /var/www/xiaosong-web && git log --oneline -5
node -v
npm -v
\\\

---

生成时间：2026-08-11 01:57:09
