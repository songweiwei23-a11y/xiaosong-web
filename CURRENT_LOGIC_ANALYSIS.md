# 当前邀请码逻辑完整分析

## 📋 新用户注册流程

### 1. 注册限制
**问题**: 新手如果没有邀请码能否注册？
**答案**: ❌ **不能！** 必须有邀请码才能注册

**流程**:
```
用户访问 /login → 点击"注册" 
  → 填写邮箱 + 密码 + 邀请码（必填）
  → 点击"注册账户"
  → 验证邀请码（调用 /api/invitation/validate）
  → 如果无效 → ❌ 阻止注册，显示错误
  → 如果有效 → ✅ 创建账户
  → 使用邀请码（调用 /api/invitation/use）
  → 设置会员等级（根据邀请码的plan_type）
  → 设置 is_legacy_user = false（新用户）
```

---

## 🎫 邀请码使用后的会员等级

根据邀请码的 `plan_type`，新用户会获得对应的会员等级：

| 邀请码类型 | 用户会员等级 | 说明 |
|-----------|-------------|------|
| free | free（体验版） | 最常见 |
| basic | basic（基础版） | 付费用户邀请 |
| pro | pro（专业版） | 付费用户邀请 |
| enterprise | enterprise（企业版） | 付费用户邀请 |

---

## 📊 新用户真实体验次数（使用邀请码后）

### 如果使用 `plan_type = free` 的邀请码（最常见）

根据数据库 `quota_plans` 表的新额度配置：

| 功能 | 新用户额度 | 说明 |
|------|----------|------|
| 脚本生成 | **3次/月** | 从10次降到3次 |
| 选题策划 | **5次/月** | 从20次降到5次 |
| 账号定位 | **3次/月** | 从15次降到3次 |
| 分镜脚本 | **2次/月** | 从8次降到2次 |
| 审稿优化 | **3次/月** | 从10次降到3次 |
| 标题封面 | **3次/月** | 从15次降到3次 |
| 自由对话 | **15次/月** | 从50次降到15次 |
| 成交理由 | **3次/月** | 从10次降到3次 |
| 知识库 | **10次/月** | 从30次降到10次 |

**降幅**: 70-80%

---

## ⚠️ 当前存在的问题

### 问题1: 额度检查逻辑可能未完全更新

**检查点1**: 现有功能是否使用新的额度检查？

让我检查一下现有代码是否已经切换到新的额度检查逻辑...

**需要验证**:
1. `lib/history.ts` 是否使用旧的 `getPlan` 函数？
2. 其他功能是否调用了新的 `checkUserQuota` 函数？
3. 数据库函数 `get_user_quota_limit` 是否正确识别新老用户？

### 问题2: 初始邀请码从哪里来？

**当前状态**:
- 数据库迁移时生成了100个初始邀请码（XS-XXXXXX格式）
- 都是 `plan_type = free`，`status = active`
- 这些码已经存在于Supabase数据库中

**获取方式**:
1. 管理员在Admin后台查看（/admin/invitations）
2. 或直接在Supabase查询：
   ```sql
   SELECT code FROM invitation_codes WHERE status = 'active' LIMIT 10;
   ```

---

## ✅ 逻辑验证计划

### 步骤1: 检查额度系统是否已连接
检查现有功能是否使用新的额度检查逻辑

### 步骤2: 执行数据库迁移
确保Supabase数据库有新表和新字段

### 步骤3: 测试完整流程
1. 获取一个有效邀请码
2. 注册新账户
3. 登录后检查额度
4. 使用各个功能，验证额度是否正确扣除

---

## 🎯 预期结果

如果一切正常：

✅ 新用户必须使用邀请码才能注册
✅ 注册后 is_legacy_user = false
✅ 新用户只能使用新的低额度（脚本3次/月等）
✅ 老用户（is_legacy_user = true）仍然享有旧的高额度

---

## 🚨 需要立即检查的事项

1. 现有功能是否连接到新的额度系统？
2. 数据库迁移是否已执行？
3. 初始邀请码是否已生成？

让我立即检查这些...
