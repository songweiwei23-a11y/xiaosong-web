# ⚠️ 重要发现：额度系统未完全整合

## 🔍 问题分析

### 问题1: 额度检查使用旧系统
**发现**: `lib/history.ts` 和其他功能仍在使用旧的 `getPlan` 函数
**影响**: 新用户实际使用的是 `lib/config/plans.ts` 中的硬编码额度，而不是数据库的新额度

**旧系统配置（lib/config/plans.ts）**:
```typescript
free: {
  quotas: {
    knowledge: -1,      // 无限
    positioning: 1,     // 一次性
    topic: 3,           // 每月
    script: 20,         // 每月 ❌ 应该是3次
    freeChat: 20,       // 每月 ❌ 应该是15次
    storyboard: 0,
    review: 0,
    title: 0,
    dealReason: 0
  }
}
```

**新系统配置（数据库 quota_plans）**:
```
script: 3次/月
topic: 5次/月
freeChat: 15次/月
... (降低70-80%)
```

### 问题2: 两套系统并存
- **旧系统**: `lib/config/plans.ts` + `getPlan` 函数
- **新系统**: 数据库 `quota_plans` + `checkUserQuota` 函数
- **当前状态**: 新系统创建了，但没有连接到实际功能

---

## 🎯 解决方案

有两个选择：

### 方案A: 更新旧配置文件（快速简单）⭐ 推荐
**优点**: 
- 5分钟完成
- 不改变架构
- 风险低

**操作**: 直接修改 `lib/config/plans.ts` 中的 free 配置

### 方案B: 全面切换到数据库系统（彻底但复杂）
**优点**: 
- 动态配置
- 管理员可在后台调整
- 更灵活

**缺点**: 
- 需要修改多个文件
- 测试工作量大
- 风险较高

---

## 💡 建议：方案A + 保留新系统

### 步骤1: 立即更新旧配置（5分钟）
修改 `lib/config/plans.ts`，将 free 配置降低到新额度

### 步骤2: 保留数据库新系统
- 数据库表和Admin管理功能保留
- 未来可以逐步迁移到新系统

### 步骤3: 清晰标注
在代码中说明当前使用旧系统，新系统待后续整合

---

## 📊 当前实际情况

### 新用户注册流程
✅ **正确**: 必须使用邀请码才能注册
✅ **正确**: 邀请码验证、使用、关系记录都正常
✅ **正确**: is_legacy_user 设置为 false

### 新用户额度
❌ **问题**: 实际使用的是旧配置
- 脚本生成: 实际20次（应该是3次）
- 选题策划: 实际3次（正确）
- 自由对话: 实际20次（应该是15次）

### 老用户额度
✅ **正确**: is_legacy_user=true 的用户使用旧配置（高额度）

---

## 🚀 立即行动

我建议：
1. **立即修改** `lib/config/plans.ts` 的 free 配置
2. **保留** 所有数据库和Admin功能（已完成的工作不浪费）
3. **测试** 修改后的额度是否生效
4. **未来** 逐步迁移到数据库系统

您同意这个方案吗？如果同意，我立即执行方案A！
