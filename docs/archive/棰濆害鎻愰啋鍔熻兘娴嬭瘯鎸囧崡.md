# 额度提醒功能测试脚本

## 1. 测试 API 端点

### 获取当前用户ID
1. 登录网站：http://122.51.234.155:3000/login
2. 打开浏览器开发者工具（F12）
3. 进入 Console 控制台
4. 执行以下代码获取用户ID：

\\\javascript
// 获取当前用户ID
const { data: { session } } = await supabase.auth.getSession();
console.log('用户ID:', session?.user?.id);
\\\

### 测试额度检查 API
在浏览器 Console 中执行：

\\\javascript
// 替换为你的用户ID
const userId = '你的用户ID';

// 调用额度检查API
fetch(\/api/quota/check?userId=\\)
  .then(res => res.json())
  .then(data => {
    console.log('额度检查结果:', data);
    console.log('警告列表:', data.warnings);
    console.log('是否用尽:', data.exhausted);
    console.log('当前套餐:', data.planName);
  });
\\\

## 2. 前端测试

### 测试额度提醒弹窗
1. 访问：http://122.51.234.155:3000/dashboard/script
2. 如果额度 ≥ 80%使用，应该自动弹出提醒弹窗
3. 检查弹窗内容：
   - ✅ 显示当前套餐名称
   - ✅ 显示各功能使用情况（进度条）
   - ✅ 显示剩余次数
   - ✅ 有"我知道了"和"升级套餐"按钮

### 测试额度用尽页面
1. 访问：http://122.51.234.155:3000/dashboard/script
2. 如果额度完全用尽，应该显示全屏阻断页面
3. 检查页面内容：
   - ✅ 显示"脚本生成功能额度已用完"
   - ✅ 显示当前套餐信息
   - ✅ 显示升级套餐优势列表
   - ✅ 有"立即升级套餐"和"返回工作台"按钮
   - ✅ 无法使用脚本生成功能

## 3. 数据库修改脚本

### 方法A：在 Supabase SQL Editor 中执行

\\\sql
-- 1. 查看当前用户的额度情况
SELECT * FROM user_quotas WHERE user_id = '你的用户ID';

-- 2. 模拟额度不足（80%使用 - 触发提醒）
UPDATE user_quotas 
SET script_used = 8,
    updated_at = NOW()
WHERE user_id = '你的用户ID';

-- 3. 模拟额度用尽（100%使用 - 触发阻断）
UPDATE user_quotas 
SET script_used = 10,
    updated_at = NOW()
WHERE user_id = '你的用户ID';

-- 4. 恢复额度（重置为0）
UPDATE user_quotas 
SET script_used = 0,
    updated_at = NOW()
WHERE user_id = '你的用户ID';
\\\

### 方法B：使用 psql 命令行

\\\ash
# SSH 到服务器后执行
psql -h <SUPABASE_HOST> -U postgres -d postgres

# 然后执行上面的 SQL 语句
\\\

## 4. 完整测试流程

### 测试场景1：额度提醒（警告）
1. ✅ 修改数据库：script_used = 8（80%使用）
2. ✅ 访问脚本生成页面
3. ✅ 应该立即弹出"额度提醒"弹窗
4. ✅ 点击"我知道了"关闭弹窗
5. ✅ 弹窗关闭后可以正常使用功能

### 测试场景2：额度用尽（阻断）
1. ✅ 修改数据库：script_used = 10（100%用尽）
2. ✅ 访问脚本生成页面
3. ✅ 应该显示全屏"额度用尽"页面
4. ✅ 无法关闭，只能点击"升级套餐"或"返回工作台"
5. ✅ 尝试生成脚本会被阻止

### 测试场景3：额度充足（无提醒）
1. ✅ 修改数据库：script_used = 0（0%使用）
2. ✅ 访问脚本生成页面
3. ✅ 不应该显示任何提醒
4. ✅ 可以正常使用功能

## 5. 检查清单

### 前端检查
- [ ] 页面加载时调用了 /api/quota/check API
- [ ] 额度提醒弹窗样式正确（黄色警告图标）
- [ ] 额度用尽页面样式正确（红色图标）
- [ ] 进度条显示正确百分比
- [ ] 按钮跳转链接正确

### API 检查
- [ ] GET /api/quota/check 返回正确数据
- [ ] warnings 数组包含所有 ≥80% 的功能
- [ ] exhausted 标识正确
- [ ] planName 显示正确

### 数据库检查
- [ ] user_quotas 表数据正确
- [ ] 各功能的 *_used 字段准确
- [ ] current_period_end 周期正确

## 6. 快速测试命令

在浏览器 Console 中一键测试：

\\\javascript
// 快速测试函数
async function testQuotaReminder() {
  const { data: { session } } = await supabase.auth.getSession();
  if (!session) {
    console.error('未登录');
    return;
  }
  
  const userId = session.user.id;
  console.log('测试用户ID:', userId);
  
  const res = await fetch(\/api/quota/check?userId=\\);
  const data = await res.json();
  
  console.log('=== 额度检查结果 ===');
  console.log('当前套餐:', data.planName);
  console.log('是否用尽:', data.exhausted);
  console.log('警告数量:', data.warnings?.length || 0);
  
  if (data.warnings?.length > 0) {
    console.table(data.warnings);
  }
  
  return data;
}

// 执行测试
testQuotaReminder();
\\\

---
生成时间：2026-08-11 01:22:07
