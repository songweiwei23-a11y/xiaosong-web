# 💳 会员收费系统使用指南

## 📋 系统概述

小宋编导工作台已完成会员收费系统的搭建，采用**固定收款码+人工审核**的方案，无需企业资质即可快速上线。

## 🎯 核心功能

### 用户端
1. **选择套餐** - 基础会员、专业会员、企业版
2. **选择支付方式** - 支付宝 / 微信支付
3. **扫码支付** - 查看收款二维码并完成支付
4. **上传凭证** - 上传支付截图
5. **等待审核** - 系统自动提醒管理员审核
6. **自动开通** - 审核通过后立即开通会员权限

### 管理员端
1. **订单审核** - 查看待审核订单、支付凭证
2. **一键通过/拒绝** - 审核通过自动开通会员
3. **收款码管理** - 上传/更新支付宝、微信收款二维码
4. **订单统计** - 查看订单数据和收入统计

---

## 🚀 首次部署步骤

### 第一步：运行数据库迁移

在 Supabase 项目的 SQL Editor 中依次执行以下迁移文件：

```bash
supabase/migrations/20240108_payment_orders.sql       # 订单表
supabase/migrations/20240108_payment_qrcodes.sql      # 二维码配置表
supabase/migrations/20240108_storage_buckets.sql      # 存储桶和权限
```

### 第二步：上传收款二维码

1. 访问 `http://localhost:3000/admin/qrcodes`
2. 准备好您的支付宝和微信个人收款二维码图片
3. 分别点击"上传二维码"按钮上传
4. 确认上传成功后，用户即可在支付页面看到

> ⚠️ **重要提示**：
> - 请使用清晰、高质量的二维码图片
> - 建议尺寸：500x500px 以上
> - 支持格式：JPG、PNG、WebP
> - 确保二维码未过期且可正常收款

### 第三步：测试完整流程

1. **用户测试**：
   - 访问 `/payment?plan=basic&cycle=monthly`
   - 选择支付方式
   - 查看二维码是否正常显示
   - 上传测试截图
   - 确认订单状态变为"待审核"

2. **管理员测试**：
   - 访问 `/admin/orders`
   - 查看测试订单
   - 点击"查看凭证"确认截图
   - 点击"通过"按钮
   - 确认会员已自动开通

---

## 🎨 页面路由

### 用户端页面
| 路由 | 说明 |
|------|------|
| `/payment` | 支付页面 |
| `/dashboard/orders` | 我的订单 |

### 管理员页面
| 路由 | 说明 |
|------|------|
| `/admin` | 管理后台首页 |
| `/admin/orders` | 订单审核 |
| `/admin/qrcodes` | 收款二维码管理 |
| `/admin/users` | 用户管理 |
| `/admin/subscriptions` | 会员管理 |

---

## 📊 数据库表结构

### payment_orders（支付订单）
```sql
- id: UUID (主键)
- user_id: UUID (关联用户)
- plan_type: TEXT (套餐类型：basic/pro/enterprise)
- plan_name: TEXT (套餐名称)
- billing_cycle: TEXT (计费周期：monthly/yearly)
- amount: NUMERIC (金额)
- payment_method: TEXT (支付方式：alipay/wechat)
- status: TEXT (状态：pending/reviewing/approved/rejected/expired)
- proof_image_url: TEXT (凭证图片)
- review_note: TEXT (审核备注)
- created_at, expires_at, reviewed_at: TIMESTAMP
```

### payment_qrcodes（收款二维码）
```sql
- id: UUID (主键)
- payment_method: TEXT (支付方式：alipay/wechat)
- qrcode_url: TEXT (二维码URL)
- is_active: BOOLEAN (是否启用)
- updated_at: TIMESTAMP
```

### subscriptions（会员订阅）
```sql
- id: UUID (主键)
- user_id: UUID (关联用户)
- plan: TEXT (套餐：basic/pro/enterprise)
- status: TEXT (状态：active/cancelled/expired)
- current_period_end: TIMESTAMP (到期时间)
```

---

## 🔐 权限控制

### RLS 策略（Row Level Security）
- **用户**：只能查看自己的订单
- **管理员**：可查看和审核所有订单
- **订单自动过期**：24小时未审核自动过期

### Storage 权限
- **payment-proofs**：用户可上传，公开可读
- **payment-qrcodes**：仅管理员可上传，公开可读

---

## 💡 最佳实践

### 审核建议
1. **及时审核**：建议在1小时内完成审核，提升用户体验
2. **验证金额**：核对支付截图中的金额是否与订单一致
3. **检查时间**：确认支付时间在订单创建后
4. **拒绝理由**：拒绝订单时务必填写详细原因

### 二维码管理
1. **定期更新**：建议每月更新一次收款二维码
2. **备份二维码**：保存本地备份以防丢失
3. **测试收款**：更新后先自行测试能否正常扫码

### 安全建议
1. **管理员权限**：严格控制管理员账号，定期审计
2. **监控异常**：关注异常订单（如金额不符、重复支付等）
3. **备份数据**：定期导出订单数据进行备份

---

## 🛠️ 常见问题

### Q1: 用户看不到收款二维码？
**A**: 检查以下几点：
- 确认已在 `/admin/qrcodes` 上传二维码
- 检查 Storage bucket 是否创建成功
- 查看浏览器控制台是否有错误

### Q2: 审核通过但会员未开通？
**A**: 检查：
- `subscriptions` 表是否有对应记录
- 用户刷新页面查看会员状态
- 查看浏览器控制台的错误信息

### Q3: 订单自动过期了怎么办？
**A**: 过期订单需要用户重新下单。可在管理后台手动修改订单状态为 `reviewing` 后再审核。

### Q4: 如何修改会员价格？
**A**: 修改 `app/payment/page.tsx` 中的 `plans` 配置对象。

### Q5: 支持企业支付接口吗？
**A**: 当前版本为个人收款码方案。如需接入企业支付，需要：
- 申请支付宝/微信企业支付资质
- 开发服务端支付API
- 实现支付回调验证

---

## 📞 技术支持

如遇到问题，请提供以下信息：
1. 具体操作步骤
2. 浏览器控制台错误信息
3. Supabase 数据库日志
4. 截图说明

---

## 🎉 完成！

现在您的会员收费系统已经完全就绪。建议先进行小范围内测，确认流程无误后再正式上线。

**祝您运营顺利！** 🚀