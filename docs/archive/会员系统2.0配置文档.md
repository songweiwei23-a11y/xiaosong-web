# 💳 会员收费系统 - 最新配置说明

## 📊 套餐方案（已更新）

### 免费版 - ¥0
**适合：** 体验用户、轻度使用者

**额度配置：**
- ✅ 知识库：**无限使用**
- ✅ 账号定位：**1次**（一次性）
- ✅ 选题策划：**3次/月**
- ✅ 脚本生成：**20次/月**
- ✅ 自由对话：**20次/月**
- ❌ 分镜脚本：0次
- ❌ 审稿优化：0次
- ❌ 标题封面：0次
- ❌ 成交理由：0次

**特点：**
- 账号定位是一次性额度，使用后不再重置
- 其他功能每月自动重置
- 知识库功能完全免费无限使用

---

### 基础会员 - ¥30/月
**适合：** 个人创作者、兼职运营

**额度配置：**
- ✅ 知识库：**无限使用**
- ✅ 所有功能：**150次/月**（统一额度）
  - 账号定位：150次/月
  - 选题策划：150次/月
  - 脚本生成：150次/月
  - 自由对话：150次/月
  - 分镜脚本：150次/月
  - 审稿优化：150次/月
  - 标题封面：150次/月
  - 成交理由：150次/月

**年付优惠：** ¥288/年（立省 ¥72）

---

### 专业会员 - ¥99/月
**适合：** 全职创作者、MCN机构

**额度配置：**
- ✅ 知识库：**无限使用**
- ✅ 所有功能：**500次/月**（统一额度）
- ✅ 数据分析报告
- ✅ 优先客服支持

**年付优惠：** ¥950/年（立省 ¥238）

---

### 企业版 - ¥199/月
**适合：** 企业团队、代运营公司

**额度配置：**
- ✅ **所有功能无限使用**
- ✅ 定制化模板
- ✅ 专属客服支持
- ✅ API接口调用
- ✅ 数据导出权限

**年付优惠：** ¥1910/年（立省 ¥478）

---

## 🔧 技术实现

### 1. 数据库设计

**新增表：`user_quotas`**
```sql
- knowledge_used      -- 知识库使用次数
- positioning_used    -- 账号定位使用次数
- topic_used          -- 选题策划使用次数
- script_used         -- 脚本生成使用次数
- free_chat_used      -- 自由对话使用次数
- storyboard_used     -- 分镜脚本使用次数
- review_used         -- 审稿优化使用次数
- title_used          -- 标题封面使用次数
- deal_reason_used    -- 成交理由使用次数
- current_period_start  -- 周期开始时间
- current_period_end    -- 周期结束时间（30天）
```

**特点：**
- 每个用户独立记录
- 每月自动重置（除免费版的账号定位）
- 支持RLS权限控制

### 2. 配置文件

**`lib/config/plans.ts`**
- 集中管理所有套餐配置
- 包含价格、功能、额度等信息
- 便于后续调整

### 3. API接口

**`/api/quota` (POST)** - 检查并扣除额度
```typescript
{
  userId: string,
  feature: 'script' | 'topic' | 'positioning' | ...
}
```

**返回：**
```typescript
{
  allowed: boolean,
  remaining: number | '无限',
  message: string
}
```

**`/api/quota` (GET)** - 查询剩余额度
```typescript
?userId=xxx
```

### 4. 客户端Hook

**`hooks/useQuota.ts`**
```typescript
const { quotaInfo, checkAndUseQuota, getRemaining, hasAccess } = useQuota();

// 使用示例
const handleGenerate = async () => {
  const { allowed, message } = await checkAndUseQuota('script');
  if (!allowed) {
    alert(message); // "免费版额度已用完，请升级会员"
    return;
  }
  // 继续生成...
};
```

---

## 🚀 部署步骤

### 第一步：运行数据库迁移

在Supabase SQL Editor中执行：

```sql
-- 1. 订单表（已有）
-- supabase/migrations/20240108_payment_orders.sql

-- 2. 二维码表（已有）
-- supabase/migrations/20240108_payment_qrcodes.sql

-- 3. 存储桶（已有）
-- supabase/migrations/20240108_storage_buckets.sql

-- 4. 额度追踪表（新增）
supabase/migrations/20240109_user_quotas.sql
```

### 第二步：配置环境变量

确保`.env.local`包含：
```env
NEXT_PUBLIC_SUPABASE_URL=your_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

**重要：** `SUPABASE_SERVICE_ROLE_KEY` 是新增的，用于服务端操作额度表。

### 第三步：集成到现有功能

在每个功能页面（脚本生成、选题策划等）添加额度检查：

```typescript
import { useQuota } from '@/hooks/useQuota';

export default function ScriptPage() {
  const { checkAndUseQuota, getRemaining } = useQuota();
  
  const handleGenerate = async () => {
    // 在调用AI生成前检查额度
    const { allowed, message } = await checkAndUseQuota('script');
    
    if (!allowed) {
      notify(message, 'error');
      // 可选：跳转到升级页面
      router.push('/pricing');
      return;
    }
    
    // 继续执行生成逻辑...
  };
  
  return (
    <div>
      <p>剩余次数: {getRemaining('script')}</p>
      <button onClick={handleGenerate}>生成脚本</button>
    </div>
  );
}
```

### 第四步：上传收款二维码

1. 访问 `/admin/qrcodes`
2. 上传支付宝和微信收款码
3. 确认显示正常

### 第五步：测试完整流程

**用户端测试：**
1. 免费用户使用脚本生成20次后提示额度不足
2. 点击升级 → 选择套餐 → 支付 → 上传凭证
3. 管理员审核通过
4. 用户刷新后额度变为150次（基础会员）

**管理端测试：**
1. 访问 `/admin/orders` 查看订单
2. 审核通过订单
3. 确认用户会员已自动开通

---

## 📋 功能集成清单

需要在以下页面添加额度检查：

- [ ] `/dashboard/script` - 脚本生成
- [ ] `/dashboard/topic` - 选题策划
- [ ] `/dashboard/positioning` - 账号定位
- [ ] `/dashboard/free-chat` - 自由对话
- [ ] `/dashboard/storyboard` - 分镜脚本
- [ ] `/dashboard/review` - 审稿优化
- [ ] `/dashboard/title` - 标题封面
- [ ] `/dashboard/deal-reason` - 成交理由

**每个页面需要：**
1. 导入 `useQuota` hook
2. 在生成前调用 `checkAndUseQuota()`
3. 显示剩余次数
4. 额度不足时引导升级

---

## 🎯 用户体验优化建议

### 1. 额度提醒
- 剩余10次时：黄色提示
- 剩余5次时：橙色警告
- 剩余0次时：红色提示 + 升级按钮

### 2. 升级引导
```tsx
{!hasAccess('script') && (
  <div className="bg-purple-50 border border-purple-200 rounded-lg p-4 mb-4">
    <p className="text-purple-800 mb-2">
      😊 本月额度已用完，升级会员继续使用
    </p>
    <Link 
      href="/pricing" 
      className="bg-purple-600 text-white px-4 py-2 rounded-lg"
    >
      查看套餐
    </Link>
  </div>
)}
```

### 3. 套餐对比页面
- 已创建：`/pricing` 页面
- 包含详细功能对比表
- 一键跳转支付

### 4. Dashboard显示
```tsx
<div className="bg-card p-4 rounded-lg">
  <h3 className="font-bold mb-2">本月使用情况</h3>
  <div className="space-y-2">
    <div>脚本生成: {quotaInfo.used.script} / {quotaInfo.quotas.script}</div>
    <div>选题策划: {quotaInfo.used.topic} / {quotaInfo.quotas.topic}</div>
    ...
  </div>
  <p className="text-sm text-muted-foreground mt-2">
    周期结束: {new Date(quotaInfo.periodEnd).toLocaleDateString()}
  </p>
</div>
```

---

## 📊 管理后台功能

### 1. 用户额度查看
在 `/admin/users` 可以查看每个用户的：
- 当前套餐
- 各功能使用情况
- 周期结束时间

### 2. 手动调整额度
可以开发管理员接口手动增加/减少用户额度

### 3. 数据统计
- 各套餐订阅人数
- 功能使用热度
- 转化率分析

---

## 💡 运营建议

### 1. 定价策略
- 免费版：降低门槛，吸引用户注册
- 基础会员¥30：与市场主流价格持平
- 专业会员¥99：核心用户群体
- 企业版¥199：高价值客户

### 2. 促销活动
- 新用户首月5折
- 年付8折优惠
- 推荐好友赠送额度

### 3. 增长策略
- 免费用户体验核心功能
- 20次脚本生成足够完成2-3个短视频
- 用完后自然产生升级需求

---

## 🔄 额度重置机制

### 自动重置
- 每30天自动重置一次
- 从开通日期起算（非自然月）
- 可通过定时任务或云函数触发

### 手动重置
管理员可执行SQL：
```sql
SELECT reset_monthly_quotas();
```

### 免费版特殊规则
- 账号定位只能用1次，永久不重置
- 升级会员后，定位额度按新套餐计算并重置

---

## 📞 技术支持

遇到问题请检查：
1. 数据库迁移是否全部执行成功
2. `SUPABASE_SERVICE_ROLE_KEY` 是否配置
3. `user_quotas` 表是否有用户记录
4. API路由是否正常响应

**调试命令：**
```bash
# 检查用户额度
curl http://localhost:3000/api/quota?userId=xxx

# 测试扣除额度
curl -X POST http://localhost:3000/api/quota \
  -H "Content-Type: application/json" \
  -d '{"userId":"xxx","feature":"script"}'
```

---

## ✅ 完成检查清单

- [x] 套餐配置文件创建
- [x] 额度数据库表设计
- [x] API接口开发
- [x] useQuota Hook
- [x] 套餐对比页面
- [ ] 各功能页面集成额度检查
- [ ] Dashboard额度显示
- [ ] 管理后台额度管理
- [ ] 定时重置任务
- [ ] 用户引导优化

---

**最后更新：** 2026-08-09  
**版本：** v2.0.0  
**重大变更：** 全新额度系统，更清晰的套餐划分