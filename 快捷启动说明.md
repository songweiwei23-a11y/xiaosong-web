# 快捷启动工具使用说明

本项目包含多个快捷脚本，方便日常开发和部署维护。

## 📁 脚本文件清单

### 1. **启动本地开发.ps1** 🚀
**功能**: 自动启动本地开发环境
- 检查项目目录
- 检查 Node.js 环境
- 检查并安装依赖
- 检查环境配置
- 启动开发服务器
- 自动打开浏览器

**使用方式**:
- 双击桌面快捷方式 **"小宋工作台-本地开发"**
- 或双击项目目录中的 **启动开发环境.bat**
- 或在 PowerShell 中运行: `.\启动本地开发.ps1`

**访问地址**: http://localhost:3000

**停止服务**: 在控制台按 `Ctrl + C`

---

### 2. **停止开发服务器.bat** ⏹️
**功能**: 快速停止所有 Node.js 进程
- 查找运行中的 Node.js 进程
- 强制终止所有 node.exe 进程

**使用方式**:
- 双击 **停止开发服务器.bat**

**注意**: 会停止所有 Node.js 进程，请确保没有其他重要的 Node 应用在运行

---

### 3. **检查项目状态.ps1** 🔍
**功能**: 全面检查项目状态
- Git 状态（分支、未提交更改）
- 依赖安装情况
- 环境配置检查
- 项目结构完整性
- 开发服务器运行状态
- Node.js 和 npm 版本

**使用方式**:
- 双击运行或在 PowerShell 中: `.\检查项目状态.ps1`

---

### 4. **构建测试.ps1** 🏗️
**功能**: 构建生产版本并测试
- 清理旧构建
- TypeScript 类型检查
- 构建生产版本
- 显示构建信息
- 可选：启动生产预览

**使用方式**:
- 在 PowerShell 中运行: `.\构建测试.ps1`

**用途**: 部署前必须运行，确保代码可以正常构建

---

### 5. **同步到服务器.ps1** ☁️
**功能**: 同步代码到腾讯云服务器
- 检查本地更改
- 检查服务器连接
- 创建压缩包
- 上传到服务器
- 自动部署和重启服务

**使用方式**:
- 在 PowerShell 中运行: `.\同步到服务器.ps1`

**前置条件**:
- SSH 密钥文件存在: `C:\Users\DELL\Desktop\song.pem`
- 服务器可访问: `122.51.234.155`
- 服务器已配置 PM2

**目标服务器**: `ubuntu@122.51.234.155:/var/www/xiaosong-web`

---

## 🔧 日常开发流程

### 启动开发环境
1. 双击桌面快捷方式 **"小宋工作台-本地开发"**
2. 等待自动检查和启动
3. 浏览器自动打开 http://localhost:3000
4. 开始开发

### 修改代码
- 修改代码后，开发服务器会自动热重载
- 浏览器会自动刷新显示最新内容

### 测试功能
- 在浏览器中测试所有功能
- 检查控制台是否有错误
- 确保所有页面正常显示

### 提交代码
```bash
git add .
git commit -m "描述你的更改"
git push
```

### 部署到服务器
1. 先运行 **构建测试.ps1** 确保可以正常构建
2. 运行 **同步到服务器.ps1** 部署到生产环境
3. 访问服务器 IP 验证部署结果

---

## ⚙️ 环境配置

### 首次使用前配置
1. 复制 `.env.example` 为 `.env.local`
2. 编辑 `.env.local`，填入正确的配置:
   - Supabase URL 和密钥
   - Dify API 密钥
   - 其他必要配置

### 示例配置
```env
NEXT_PUBLIC_SUPABASE_URL=https://你的项目.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的匿名密钥
SUPABASE_SERVICE_ROLE_KEY=你的服务密钥
DIFY_API_KEY=你的Dify密钥
DIFY_API_URL=https://api.dify.ai/v1
```

---

## 🐛 常见问题

### 1. 启动失败 - 端口被占用
**问题**: `Error: Port 3000 is already in use`
**解决**: 运行 **停止开发服务器.bat** 或更换端口

### 2. 依赖安装失败
**问题**: `npm install` 报错
**解决**: 
- 删除 `node_modules` 和 `package-lock.json`
- 重新运行 `npm install`

### 3. 环境配置未找到
**问题**: `未找到 .env.local 文件`
**解决**: 
- 复制 `.env.example` 为 `.env.local`
- 填入正确的配置信息

### 4. TypeScript 错误
**问题**: 类型检查失败
**解决**: 
- 检查代码中的类型错误
- 运行 `npm run type-check` 查看详细错误

### 5. SSH 连接失败
**问题**: 无法连接到服务器
**解决**: 
- 检查 SSH 密钥路径是否正确
- 确认服务器 IP 和用户名
- 测试网络连接

---

## 📝 脚本自定义

### 修改服务器信息
编辑 **同步到服务器.ps1**，修改以下变量:
```powershell
$serverIP = "你的服务器IP"
$serverUser = "你的用户名"
$serverPath = "你的项目路径"
$sshKey = "你的SSH密钥路径"
```

### 修改端口
编辑 **启动本地开发.ps1**，在启动命令前添加:
```powershell
$env:PORT = "3001"
```

---

## 🎯 快捷键提示

- **Ctrl + C**: 停止开发服务器
- **Ctrl + Shift + R**: 浏览器强制刷新（无缓存）
- **F12**: 打开浏览器开发者工具

---

## 📞 技术支持

如有问题，请检查:
1. Node.js 版本 >= 18
2. npm 版本 >= 9
3. Git 已安装并配置
4. 环境变量正确配置

---

**最后更新**: 2026-09-18
**维护者**: 小宋工作台团队
