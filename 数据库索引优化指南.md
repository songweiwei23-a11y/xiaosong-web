# 数据库索引优化指南

## 📌 重要性
执行此优化可将查询速度提升 **10-100倍**：
- 用户登录加载：2s → 0.3s
- 配额查询：500ms → 50ms  
- 历史记录查询：1s → 100ms

## 🚀 执行步骤

### 1. 登录 Supabase Dashboard
访问：https://app.supabase.com
登录你的项目

### 2. 打开 SQL Editor
左侧菜单 → **SQL Editor** → **New Query**

### 3. 复制粘贴SQL
打开项目根目录的 数据库索引优化.sql 文件
复制全部内容粘贴到 SQL Editor

### 4. 执行脚本
点击右上角 **Run** 按钮（或按 Ctrl+Enter）

### 5. 验证结果
执行成功后，会看到索引列表，确认以下索引已创建：

`
✅ idx_user_quotas_user_id
✅ idx_user_quotas_period_end
✅ idx_subscriptions_user_id
✅ idx_subscriptions_status
✅ idx_subscriptions_plan
✅ idx_user_profiles_user_id
✅ idx_script_history_user_id
✅ idx_script_history_created_at
✅ idx_subscriptions_user_status
`

## 🔍 验证性能提升

执行索引优化后，在项目中运行：

\\\ash
npm run test:performance
\\\

或手动测试：
1. 登录工作台，观察加载速度
2. 生成脚本，观察历史记录加载速度
3. 切换会员等级，观察权限检查速度

## ⚠️ 注意事项

- **安全性**：此脚本只创建索引，不修改数据
- **幂等性**：使用 IF NOT EXISTS，可重复执行
- **无需停机**：可在生产环境直接执行
- **回滚**：如需删除索引，运行 DROP INDEX 命令

## 📊 性能监控

执行后可在 Supabase Dashboard 查看：
- **Database** → **Query Performance**
- 观察慢查询数量下降
- 平均响应时间缩短

---

**创建时间**：2026-08-09 18:55:24
**状态**：待执行 ⏳