# 🚀 快速开始

## 方式一：自动安装（推荐）

双击运行 `install.bat`，脚本会自动：
1. 检查 Node.js 是否已安装
2. 安装项目依赖
3. 检查环境变量配置
4. 启动开发服务器

## 方式二：手动安装

### 第1步：安装 Node.js

1. 访问 https://nodejs.org/zh-cn
2. 下载 LTS 版本（v20.x）
3. 运行安装程序（保持默认选项）
4. 安装完成后，打开新的 PowerShell

验证安装：
```powershell
node --version  # 应显示 v20.x.x
npm --version   # 应显示 10.x.x
```

### 第2步：安装依赖

```powershell
cd "C:\Users\30430\Desktop\编导知识大全\小宋\xiaosong-web"
npm install
```

如果很慢，使用国内镜像：
```powershell
npm config set registry https://registry.npmmirror.com
npm install
```

### 第3步：配置 API 密钥

编辑 `.env.local`，填入你的 Dify API 密钥：
```env
DIFY_API_KEY=app-你的密钥
```

### 第4步：启动项目

```powershell
npm run dev
```

浏览器访问：http://localhost:3000

## ✅ 成功标准

- 首页加载正常
- 点击"免费试用"进入生成页面
- 填写信息后能生成脚本
- 看到流式输出效果
- 历史记录功能正常

## 🐛 遇到问题？

查看 `安装指南.md` 或 `README.md` 中的常见问题解决方案。

---

**准备好了吗？开始安装吧！** 🎉
